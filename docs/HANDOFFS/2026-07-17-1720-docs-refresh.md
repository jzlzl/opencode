# 交接：文档刷新 (Docs Refresh)

## 任务 (Task)

优化工作区文档，让新 Codex 会话能更清楚地从规则、项目地图、命令索引进入任务。

## 已完成 (Completed)

- 将 `README.md` 重构为简洁入口页。
- 将 `README.zh-CN.md` 重构为中文入口页。
- 更新 `docs/PROJECT_MAP.md`，明确阅读顺序和后续入口。
- 更新 `docs/COMMANDS.md`，让它专注承担命令索引职责。
- 新增本交接记录。

## 未修改 (Not Changed)

- 没有修改 `token-store/` 或 `token-store-video/` 的业务代码。
- 没有修改 lockfile、环境文件或部署配置。
- 没有改变主题切换实现。
- 没有处理 `.vscode/` 未跟踪状态。

## 验证 (Verification)

- 回读更新后的 Markdown 文件，确认引用链一致。
- 检查 `git status`，确认修改范围集中在文档。

## 风险 (Risks)

- 文档价值依赖后续会话是否实际按阅读顺序执行。
- `PROJECT_MAP` 和 `COMMANDS` 必须跟随项目结构和脚本变化同步维护。

## 下一步 (Next Step)

后续会话应以 README 作为第一入口，以 `docs/PROJECT_MAP.md` 做定位，以 `docs/COMMANDS.md` 查命令。
