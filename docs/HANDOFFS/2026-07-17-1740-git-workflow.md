# 交接：Git 工作流文档 (Git Workflow Doc)

## 任务 (Task)

新增专门说明如何提交并推送到 GitHub 的文档，并把它挂到现有文档入口。

## 已完成 (Completed)

- 新增 `docs/GIT_WORKFLOW.md`。
- 在 `README.md`、`README.zh-CN.md`、`docs/PROJECT_MAP.md`、`docs/COMMANDS.md` 中加入 Git 工作流入口。
- 在 `AGENTS.md` 中加入提交、push、commit、GitHub、PR 相关任务优先读取 `docs/GIT_WORKFLOW.md` 的规则。

## 未修改 (Not Changed)

- 没有修改应用源码。
- 没有修改 lockfile、环境文件或部署配置。
- 没有处理 `.vscode/` 未跟踪状态。

## 验证 (Verification)

- 回读更新后的 Markdown 文件。
- 确认仓库已有 GitHub `origin` remote。

## 风险 (Risks)

- 文档目前是轻量流程，默认当前 GitHub remote 和分支约定不变。
- 如果后续改为强制 PR 流程，应同步更新 `docs/GIT_WORKFLOW.md`。

## 下一步 (Next Step)

后续提交、commit、push、GitHub、PR 相关任务应先参考 `docs/GIT_WORKFLOW.md`。
